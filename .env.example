# ══════════════════════════════════════════════════════════════════════════════
# Insider Threat Detection System — Environment Configuration
# ══════════════════════════════════════════════════════════════════════════════
#
# Copy this file to .env and adjust values for your environment.
# All values shown here are the defaults used when a key is absent.
#
# Shell environment variables always take priority over this file.
# ──────────────────────────────────────────────────────────────────────────────

# ── Data Generation ───────────────────────────────────────────────────────────
NUM_USERS=200          # Number of synthetic employees to generate
NUM_DAYS=60            # Simulation period in days
ANOMALY_RATE=0.04      # Fraction of event rows that are anomalous (0–1)
SEED=42                # Global random seed for reproducibility

# ── Isolation Forest ──────────────────────────────────────────────────────────
IF_N_ESTIMATORS=200    # Number of trees
IF_CONTAMINATION=0.05  # Expected anomaly fraction seen during training (0–0.5)

# ── One-Class SVM ─────────────────────────────────────────────────────────────
SVM_KERNEL=rbf         # Kernel type: rbf | linear | poly | sigmoid
SVM_NU=0.05            # Upper bound on train-set outlier fraction (0–1)
SVM_GAMMA=scale        # Kernel coefficient: scale | auto | float

# ── Autoencoder (MLPRegressor) ────────────────────────────────────────────────
AE_MAX_ITER=500        # Maximum training iterations
AE_LEARNING_RATE=0.001 # Adam initial learning rate

# ── Risk Scoring Weights (must sum to 1.0) ────────────────────────────────────
WEIGHT_IF_SCORE=0.30
WEIGHT_SVM_SCORE=0.25
WEIGHT_AE_SCORE=0.15
WEIGHT_SENSITIVE_TOTAL=0.20
WEIGHT_FAILED_TOTAL=0.10

# ── Risk Level Thresholds (0–100 scale) ───────────────────────────────────────
RISK_CRIT_THRESHOLD=75
RISK_HIGH_THRESHOLD=50
RISK_MEDIUM_THRESHOLD=25

# ── Logging ───────────────────────────────────────────────────────────────────
LOG_LEVEL=INFO         # DEBUG | INFO | WARNING | ERROR | CRITICAL
LOG_TO_FILE=true       # Write logs to logs/insider_threat.log (true | false)
